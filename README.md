🤖 Termux AI ProThe Ultimate CLI AI Client for Termux. > Chat with GPT-4, Gemini, DeepSeek, and Local Models with a beautiful, rich terminal interface.Termux AI Pro is a powerful Python application designed to replace simple shell scripts. It brings a full-featured AI chat experience to your Android terminal with Markdown rendering, streaming responses, and universal provider support.✨ Features🎨 Beautiful UI: Uses the Rich library to render Markdown, code blocks, tables, and lists perfectly.⚡ Real-time Streaming: Watch the AI type the answer instantly (no waiting for the full response).🌍 Universal Support: Pre-configured for:OpenRouter (Access DeepSeek, Claude 3, Llama 3)Google Gemini (Free tier available)OpenAI (GPT-4o, GPT-3.5)Local/Ollama (Connect to your PC or run locally)💾 Session Context: The AI remembers what you said earlier in the conversation.⚙️ Easy Config: Built-in settings menu to swap keys and models without editing files.🚀 Installation⚠️ IMPORTANT SETUPBefore installing, make sure to replace YOUR_USERNAME in the commands below with your actual GitHub username and Termux-AI-Pro with your actual repository name.Option 1: Standard Clone & Install (Recommended)This method ensures you have a copy of the repository locally for easy updates.Install Git and Python:pkg update && pkg upgrade -y
pkg install git python -y
Clone the Repository:git clone [https://github.com/YOUR_USERNAME/Termux-AI-Pro.git](https://github.com/YOUR_USERNAME/Termux-AI-Pro.git)
cd Termux-AI-Pro
Run the Installer Script:chmod +x install.sh
./install.sh
Option 2: The One-Line InstallerThis method is quick and downloads the installer directly from your repository.Copy and paste this into Termux:curl -sL [https://raw.githubusercontent.com/YOUR_USERNAME/Termux-AI-Pro/main/install.sh](https://raw.githubusercontent.com/YOUR_USERNAME/Termux-AI-Pro/main/install.sh) | bash
🎮 UsageOnce the installation is complete, you can launch the AI from anywhere in your terminal:termux-ai
In-Chat CommandsType your message: Press Enter to send.menu: Open the settings menu to change API keys, models, or providers.clear: Wipe the current conversation memory (start fresh).exit: Close the application.🔑 Setup & ConfigurationThe first time you run termux-ai, you will be prompted to set up your API key.Getting an API KeyYou need an API key to chat (unless using the "Local/Ollama" option).Free/Paid: OpenRouter (Recommended for accessing various models)Free: Google AI Studio (for Gemini keys)Paid: OpenAI PlatformEntering the KeyRun termux-ai.Type menu and press Enter.Select [2] Edit Configuration.Select the provider you want to use (e.g., OpenRouter).Paste your API Key when prompted and update the Model if necessary.🏠 Using Local AI (Ollama)You can connect Termux to an Ollama server running on your PC or another machine on your network.Ensure Ollama is Externally AccessibleOn the machine running Ollama, ensure it is served to your local network (e.g., by setting OLLAMA_HOST=0.0.0.0).Configure Termux AIRun termux-ai and type menu.Select [1] Switch Provider -> Local (Ollama).Go to [2] Edit Configuration.Set Base URL to: http://[YOUR_PC_IP_ADDRESS]:11434/v1Set Model to: The model you have pulled (e.g., llama3).🛠️ Troubleshooting"Command not found" after install?Restart your Termux session or run: hash -r."Connection Error"?Verify your API key is correct in the menu.Check your internet connection.If using a remote model, ensure you have credits or access permissions.Configuration PathYour user configuration and settings are stored here: ~/.config/termux_ai_pro/config.json.📝 LicenseThis project is licensed under the MIT License.
